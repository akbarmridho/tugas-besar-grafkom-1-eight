import {
  createIcons,
  Square,
  RectangleHorizontal,
  Slash,
  Pentagon
} from 'lucide';

export const initializeIcons = () => {
  createIcons({
    icons: {
      Square,
      RectangleHorizontal,
      Slash,
      Pentagon
    }
  });
};
