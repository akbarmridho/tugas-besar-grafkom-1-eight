import {
  createIcons,
  Square,
  RectangleHorizontal,
  Slash,
  Pentagon,
  MousePointer
} from 'lucide';

export const initializeIcons = () => {
  createIcons({
    icons: {
      Square,
      RectangleHorizontal,
      Slash,
      Pentagon,
      MousePointer
    }
  });
};
